apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "affine-cloud.fullname" . }}-test-connection"
  labels:
    {{- include "affine-cloud.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "affine-cloud.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
